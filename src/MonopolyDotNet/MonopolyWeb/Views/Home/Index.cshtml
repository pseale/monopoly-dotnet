@using Microsoft.Web.Mvc
@using MonopolyWeb.Controllers

@if (User.Identity.IsAuthenticated)
{
  Html.RenderAction((GameController x) => x.Index(null));
}
else
{
  Html.RenderAction((NewGameController x) => x.Index());
}
